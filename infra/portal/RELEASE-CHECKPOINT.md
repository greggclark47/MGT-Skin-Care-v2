# AI release controls checkpoint

2026-09-24 rendered accessibility markup checkpoint: The production web proxy journey verifies each of the 29 portal routes exposes the shared Skip to content link, primary-navigation identity and label, main landmark, labeled policy navigation, and alternative text on rendered images. The check runs against the production build and preserves the logo, visual layout, routes and application behavior. Every local verification gate passed at `work/verification/2026-09-24T15-39-35-174Z/report.md`. This is a markup regression gate, not proof of keyboard traversal, screen-reader announcements, contrast, zoom/reflow, or real-device/WCAG conformance; production remains `NOT READY`.

2026-09-24 shared navigation focus checkpoint: The portal shell moves focus to the main landmark after an in-portal route change. Opening the mobile menu focuses its first primary destination; Escape closes the menu and returns focus to Menu. The existing Skip to content link, reduced-motion behavior, logo, routes and layout remain unchanged. Type checks and every full local verification gate passed at `work/verification/2026-09-24T15-25-36-407Z/report.md`. Real screen-reader, keyboard, mobile-device, contrast and WCAG audit evidence remains `NOT READY`.

2026-09-24 support escalation checkpoint: Superadmin and compliance operators can select one fixed internal escalation reason while updating a request, which assigns the request to that operator. Supported reasons are content safety, account/privacy, billing scope, retailer purchase, technical issue, specialist review and other. Customer responses and account exports exclude the assignment and escalation reason. The operations dashboard reports aggregate reason selections only. Invalid reasons are rejected. Focused API, privacy, authorization, analytics and web checks passed, followed by every full local verification gate at `work/verification/2026-09-24T15-05-17-111Z/report.md`. This is a local workflow record; named support ownership, external delivery and production readiness remain `NOT READY`.

2026-09-24 assistant journey measurement checkpoint: Guidance outcomes are counted by the four bounded assistant roles, and next-step selections accept only six approved internal portal destinations. The authorized operations view reports aggregate guidance volume and the offered → Support opened → request saved funnel. Arbitrary action names, roles and external URLs are rejected. The metrics exclude customer questions, identities, account details, email addresses and ticket content, use the existing 180-day aggregate retention policy, and do not claim unique users or guaranteed support outcomes. Focused API, authorization, analytics and web checks passed, followed by every full local verification gate at `work/verification/2026-09-24T14-47-37-498Z/report.md`. Human support ownership, external delivery and production readiness remain `NOT READY`.

2026-09-24 referral engagement baseline checkpoint: The Shop records only daily aggregate counts for allowlisted retailer destination opens and saved-list changes. Superadmin and compliance roles can inspect a 30-day retailer and segment summary; ordinary and unauthenticated users cannot. The counters exclude identity, profiles, searches, free text, retailer-site activity, orders, amounts, revenue and profit, and default to 180-day retention. They describe portal actions rather than unique customers, retailer purchases, conversion, revenue or profit. Focused API, authorization, retention, analytics and web checks passed, followed by every full local verification gate at `work/verification/2026-09-24T14-36-10-238Z/report.md`. Partner agreements, affiliate attribution, retailer-side conversion data and production readiness remain `NOT READY`.

2026-09-23 three-phase Support operations checkpoint: Phase B1 adds five server-validated request types and distinguishes direct form intake from an explicit guidance handoff. Phase B2 adds operator assignment, open/in-review/waiting/resolved states, timestamped replies and customer-safe request responses. Phase D1 adds aggregate-only daily counters and a 30-day operations view for guidance, handoffs, request categories and workflow updates. Focused API, authorization, assistant-policy and analytics checks passed; a browser save verified the selected onboarding category, open state, shared reference and timestamp. All local release gates passed at `work/verification/2026-09-23T16-17-59-368Z/report.md`. External delivery, named staffing, response-time commitments and production readiness remain `NOT READY`.

2026-09-23 Support request-reference checkpoint: `POST /api/hub/support` now returns a server-generated request ID, creation time and open status. The Support save confirmation, customer history and authorized operator view share that ID. A local browser save confirmed the customer-facing receipt and list match; the portal integration check covers server-generated identity and another customer's isolation. All local release gates passed at `work/verification/2026-09-23T15-54-31-331Z/report.md`. Records in the current demo are local and in memory; external delivery, support ownership and production readiness remain `NOT READY`.

2026-09-23 Support handoff draft checkpoint: A customer-care handoff can now copy the submitted question into the existing Portal Support request form only after the user selects an explicit action. The subject receives focus for review; no ticket is created until Save request. A focused browser flow confirmed zero requests before Save and one saved local request afterward, including protection against copying later edits to the unsent question. The full local gate passed at `work/verification/2026-09-23T15-39-30-863Z/report.md`; web typecheck and production build also passed after the final UI edit. External support delivery and production readiness remain `NOT READY`.

2026-09-23 target repository CI checkpoint: GitHub Actions run [35881676411](https://github.com/greggclark47/MGT-Skin-Care-v2/actions/runs/35881676411) passed for the initial portal import. The Support handoff edit was made afterward and needs its own PR-head CI run.

2026-09-23 CI bootstrap checkpoint: The workflow now installs the pinned pnpm 9.0.0 package manager before `actions/setup-node` configures pnpm store caching. The focused CI contract suite passes all three cases. This is a repository-level fix only; a hosted GitHub Actions run has not yet been observed and production remains `NOT READY`.

2026-09-23 clean-checkout verification checkpoint: The GitHub repository integration worktree exposed a release-gate dependency on a pre-existing root `dist` folder. `pnpm build:static` now builds the configured static preview from tracked sources, and the local verifier runs that build before scanning public artifacts. The source checkout's full local verification passed at `work/verification/2026-09-23T15-15-12-332Z/report.md`. This corrects a clean-checkout CI blocker; live services, hosted CI and production readiness remain unverified.

2026-09-23 Phase 11 Support accessibility checkpoint: Commit `f075a43` adds focus management for new guidance results and errors, a focusable labeled Support request destination, and an explicit busy state. Web production build, 28-page proxy journey, and focused browser accessibility-tree checks of answer, handoff, and sign-in error focus passed. Shared branding, layout, routes, and ticket-submission boundary are unchanged. Full keyboard, screen-reader, mobile, contrast, and WCAG review remain open; release status is `NOT READY`.

2026-09-23 Phase 10 assistant policy checkpoint: Commit `08c9ca4` adds 18 engineering candidate API cases and strengthens adverse/urgent reaction, privacy, retailer-payment, and prompt-injection routing. Deterministic cases make no AI or payment-provider call. Missing reviewed knowledge and rejected excerpts return a Support handoff without creating a ticket. All local verification gates passed at `work/verification/2026-09-23T01-51-10-062Z/report.md`, including the production web build, 28-page proxy journey, and zero-hit public artifact scan. SME/privacy sign-off, approved catalog data, full accessibility review, live services, and production configuration remain open; release status is `NOT READY`.

2026-09-23 Phase 9 Support-page guidance checkpoint: Commit `518274b` adds the bounded assistant entry to the existing `/support` layout. A general getting-started question routes to Skin Match, and a retailer-discovery question returns a Shop link without AI consent or a provider call. Guidance does not create a ticket or confirm a payment. API compilation and portal checks, web production build, the 28-page proxy journey, and focused browser interaction passed. The MGT logo and shared layout are unchanged. External support delivery, OpenClaw activation, full accessibility review and production service validation remain open; production is `NOT READY`.

2026-09-23 Phase 9 assistant foundation checkpoint: Commit `8444aa7` adds four bounded server-side assistant roles. Deterministic onboarding, support, account, payment and medical guidance avoids provider calls; signed-in, consented routine/product requests reuse the reviewed Coach gateway. API compilation, focused portal/Coach checks, full local verification, web production build, proxy journey and zero-hit public artifact scan passed. Evidence: `work/verification/2026-09-23T01-18-44-476Z/report.md`. OpenClaw remains disabled, no customer-facing assistant UI or external support delivery was enabled, and production remains `NOT READY` because `infra/portal/.env` and live service evidence are absent.

2026-09-23 Phase 8 analytics checkpoint: Commit `3a7416a` completes the analytics SDK package contract and adds its smoke suite to `pnpm release:checkpoint`. Unknown events, malformed properties and undefined required values are rejected; registry privacy/retention classes and identity stitching remain enforced. All seven local checkpoint gates passed; production preflight is blocked because `infra/portal/.env` is absent; release decision is `NOT READY`. Evidence: `work/checkpoints/2026-09-23T00-08-34-590Z/report.md` and `work/verification/2026-09-23T00-08-39-783Z/report.md`.

2026-09-22 Phase 7 local release checkpoint: Commit `ce027d4` hardens `pnpm release:checkpoint` to clean both `.next` and `.next-dev` output before verification. Local gates passed; production preflight is blocked because `infra/portal/.env` is absent; release decision is `NOT READY`. Evidence: `work/checkpoints/2026-09-22T22-49-39-671Z/report.md` and the timestamped verification report linked from that checkpoint. Run `pnpm release:checkpoint -- --require-production` to fail closed until production configuration is available.

2026-09-22 phases 4–6 continuation checkpoint: Commit `8b3b84e` advances the local commercial/referral, deployment/edge, and browser/mobile/accessibility gates. The full proxy journey now renders 28 portal routes and checks the shared MGT mark and main landmark on every route; the mobile Expo contract verifies shared questionnaire/profile use, API environment configuration, accessibility roles, and 44px tap targets. Infrastructure preflight, Compose/Dockerfile contracts, migration lineage, production build, proxy journey, mobile contract, and zero-hit public artifact scan all pass. Evidence: `work/verification/2026-09-22T22-32-48-419Z/report.md`. Device builds, staging authentication, live provider services, container startup, and deployment remain open.

2026-09-22 phases 1–3 continuation checkpoint: Commit `13d0d2e` advances the local application/data, operations/privacy, and AI/output gates. Expired guest-profile merge choices are pruned by the worker and removed during account deletion; a versioned five-case reviewed-output set covers Skin Match, Routine Builder, Care Coach, product explanations, and operator analysis with zero blocked-claim or invalid-citation failures. The complete local verification gate passed, including the production proxy journey and zero-hit public artifact scan. Evidence: `work/verification/2026-09-22T22-23-03-234Z/report.md`. Live PostgreSQL/RLS, identity provider, backup restoration, hosted/local provider runtime, and staging browser evidence remain open.

2026-09-22 guest profile merge checkpoint: The previously fail-safe guest/account conflict now has an explicit 15-minute merge decision. A verified sign-in records a pending choice without silently changing either profile; the account page lets the user keep the browser profile or the account profile, then links the session and migrates only after that choice. API build, profile lifecycle regressions, web typecheck, web smoke, and production web build pass. Live Supabase identity behavior and PostgreSQL concurrency remain environment-specific release gates.

2026-09-22 readiness continuation checkpoint: Added the read-only `/api/hub/admin/catalog-health` operator report and the phased `BUILD-READINESS-GUIDE.md`. The report checks required routine-slot coverage, active/approved/sample counts, missing ingredient data and approved SME-rule coverage without exposing customer or vendor details. API, web and portal regression validation remains required after this change; live catalog import, PostgreSQL/RLS, provider, backup and deployment gates remain environment-specific.

2026-09-20 CI branch-target checkpoint: Continuous verification now targets the repository's actual default branch, `main`, rather than the retired local branch name. The CI contract, preflight, Compose/Dockerfile, and migration-lineage checks pass; the v1.6 full local release gate also passed with evidence at `work/verification/2026-09-20T21-54-42-531Z/report.md`. GitHub-hosted execution remains pending until the pull request is merged because the workflow is new to the default branch.

2026-09-20 Git-history reconciliation checkpoint: The verified portal branch was merged with the repository's unrelated `main` history on a dedicated integration branch. Conflicting source files retain the newer verified implementation; historical root archive artifacts remain in the prior remote history and were intentionally excluded from the active source branch. The v1.6 local release gate passed after reconciliation with evidence at `work/verification/2026-09-20T21-30-28-146Z/report.md`. This does not validate live services, containers, deployment, or production data.

2026-09-20 continuous-verification checkpoint: The repository now runs the v1.6 secret-free local release gate on pull requests and pushes to `main`. The workflow installs the lockfile, validates infrastructure, Compose/Dockerfile, CI, and migration contracts, then runs the full local verification harness. It intentionally has read-only repository permission and does not deploy, accept production credentials, contact external services, or claim live readiness.

2026-09-20 deployment-input hardening checkpoint: API and web application images now require an approved immutable Node base-image digest alongside the existing Ollama and Caddy image controls. Compose passes `NODE_IMAGE` into all application builds, preflight rejects floating Node tags, and contract coverage verifies every Dockerfile consumes the pinned input. Live image resolution and container builds remain untested because Docker is unavailable in this environment.

2026-09-20 OpenClaw/Ollama compute checkpoint: The optional OpenClaw route now defaults to Ollama's native `/api/chat` contract, with the older Chat Completions-compatible route gated behind `OPENCLAW_API_MODE=openai-completions`. Compose points the API at the internal Ollama service, model synchronization remains profile-gated, and preflight plus adapter smoke coverage exercise both modes. The full local verification gate passed with evidence at `work/verification/2026-09-20T20-53-14-765Z/report.md`, including a zero-hit generated public-artifact vendor scan. Docker is unavailable in this environment, so live container startup, model pulls, OpenClaw tool-calling, and production image validation remain open release gates.

2026-09-20 portal boundary continuation checkpoint: Continued from the Claude handoff package against the actual repository state. The boundary pass removes internal billing identifiers and source fields from authenticated order exports, admin order payloads, subscription exports, and operator reconciliation responses; webhook failures are normalized to product-owned service errors. The versioned v1.5 local verification gate passes: evidence is `work/verification/2026-09-20T16-58-50-872Z/report.md`. Live PostgreSQL/RLS, imported-store reconciliation, real external services, browser/WCAG audit, deployment, repository push, and Drive backup remain open release gates.

2026-09-20 demo v2.10 checkpoint: The dark retail demo now reflects the v1.6 local release state with an interactive release-evidence view, continuous-verification readiness, and a clean public-surface scan. It distinguishes verified local results from the still-required live-environment work: container startup, data reconciliation, backup restoration, and production service checks. JavaScript syntax, release-contract checks, and the full v1.6 local verification suite passed. Evidence: `work/verification/2026-09-20T22-06-17-718Z/report.md`. This is not a production deployment claim.

2026-09-20 portal continuation checkpoint: Continued from the Claude handoff package against the actual repository state. The versioned v1.4 local verification gate passes end to end: domain, gateway, shared client, API, web smoke, deterministic policy cases, HTTP/persistence regressions, production web build, proxy journey, and generated-public-artifact scan. The fresh public scan found zero configured provider/model-name matches. The portal keeps the dark retail structure and USD prices as placeholders, adds owner-approved 30-day guest-access controls to the demo, keeps client-facing analysis and billing language product-owned, and aligns matching-owner entitlements with the approved three-day past-due grace. Evidence: `work/verification/2026-09-20T16-28-50-527Z/report.md`. Live PostgreSQL/RLS, imported-store reconciliation, real external services, browser/WCAG audit, deployment, repository push, and Drive backup remain open release gates.

2026-09-18 infrastructure preflight checkpoint: The deployment scaffold now has a tested production environment validator that fails closed on placeholder or unsafe origins, database and Supabase configuration, worker/backup timing, webhook delivery, and partially enabled subscription billing. The static end-to-end demo now includes guided Skin Match, Routine Builder, Care Coach, account-export, and infrastructure-readiness flows while preserving the original shop-first retail structure. The Caddy edge adds compression, baseline security headers, direct health/readiness/API routing and JSON access logs; Compose adds web health gating, orderly Node shutdown, and log rotation. Live container startup, production credentials, database migration, provider calls, and backup restoration remain environment-specific release gates.

2026-09-18 subscription webhook reconciliation checkpoint: Every signature-verified subscription webhook now creates or updates a durable sanitized receipt around processing. The receipt tracks attempts, duplicate deliveries, processed/ignored/failed state, safe failure code and timing without storing customer or payment payloads; invalid signatures are not recorded. Superadmin and compliance operators can inspect 24-hour event, delivery, duplicate, failure and stalled counts plus the latest safe receipts in Portal operations. Receipts expire after 90 days by default. API compilation, billing signature/idempotency/failure tests, operations retention tests, role authorization, privacy-redaction assertions and web type checking passed locally. Real Stripe delivery retries and production PostgreSQL concurrency remain unvalidated.

2026-09-18 privacy operations checkpoint: Superadmin and compliance roles now have a read-only account-deletion queue in Portal operations. The dedicated API returns due, blocked, processing and recent completion totals plus blocker categories, but intentionally excludes account IDs, emails, raw provider errors, support text and profile data. Raw deletion requests were removed from the broad admin payload. Anonymous completion proof now has a configurable 730-day retention policy. API authorization, privacy redaction, worker retention, portal integration, and web build checks passed locally; live Supabase identity deletion and legal-retention review remain required.

2026-09-18 account deletion checkpoint: Signed-in users can now schedule deletion with a 30-day cancellation period, inspect its status, include it in their data export, or cancel before processing starts. The operations worker blocks unsafe erasure when billing, open orders, operator access, vendor ownership, or unresolved hosted-AI charges remain. Eligible jobs delete the Supabase sign-in identity before transactionally removing portal personal data and sessions; retained transaction and audit records are anonymized, and completion proof contains no account identifier. The UI includes an explicit confirmation dialog and status. API compilation, deletion-worker regression tests, portal integration checks, and web type checking passed locally. A real Supabase service-role deletion, production PostgreSQL concurrency, and legal-retention review remain required before launch.

2026-09-18 operations readiness checkpoint: API liveness and operational readiness are now separate. The API container probes `/healthz`, while production `/readyz` returns 503 when the operations worker has not completed recently or verified backup status is unhealthy. The worker container uses a successful-run heartbeat instead of inheriting the API HTTP probe. Readiness selects the newest completed run by timestamp and rejects future timestamps. Backup verification also rejects future completion times. The domain pipeline smoke now fails the process on a failed assertion and isolates its PM-only retinol placement check from sensitivity exclusions. API compilation, worker/readiness, referral, billing, domain pipeline, and web production-build checks passed. Docker is unavailable in this environment, so the images and Compose stack remain unbuilt here; production PostgreSQL, real backup restoration, and container orchestration still require live validation.

2026-09-14 AI economics checkpoint: The operations admin now exposes a read-only AI economics panel backed by `/api/hub/admin/ai-economics`. Superadmin and compliance can review 24-hour request volume, success/fallback/validation rates, model-level token cost, pending hosted-cost holds, stale holds, and alert text without making provider calls or exposing customer content. API compilation, web type checking, the referral portal integration suite, and the production web build passed locally. Live OpenAI billing reconciliation and production database behavior still require provider-side validation.

2026-09-13 operator access checkpoint: A superadmin can now find an already signed-in account by exact email and assign portal roles in `/admin/operators`. The API requires a verified session, origin, CSRF, explicit confirmation, and a current role revision and role set. It blocks self-edits and ambiguous email matches, rechecks superadmin authority inside the role-change transaction, and writes an audit entry plus prior/new role record. Existing sessions pick up grants and revocations on their next request. The first superadmin still requires controlled provisioning outside the portal. Local API, web, and authorization checks passed; live Supabase/Postgres validation remains outstanding.

2026-09-13 reconciliation history checkpoint: The restricted operator AI page now shows the 50 most recent verified hold reconciliations, including reserved and actual cost, provider billing reference, operator, and time. Superadmin and compliance can review this history; only superadmin can reconcile. The pending list refreshes after a successful reconciliation. API compilation, operator and budget tests, web type checking, and the production web build passed locally. Provider invoices and production database behavior still require live validation.

2026-09-12 reservation reconciliation checkpoint: The operator AI page lists pending hosted-cost holds older than ten minutes. A superadmin can reconcile one after entering the verified provider cost and billing reference and confirming the record was checked; zero-cost reconciliation requires an explicit no-charge confirmation. The transaction updates the original UTC budget day, marks the reservation settled, and saves a separate reconciliation record. Repeated or premature reconciliation fails. Compliance can inspect holds but cannot reconcile them. API/build, budget, operator authorization, and web production-build checks passed locally. This is a manual billing reconciliation workflow; it does not verify provider invoices automatically or resolve missing live provider/database validation.

2026-09-12 operator analysis checkpoint: The operations portal now has an AI analysis page and POST /api/hub/admin/ai/analyze. Only signed-in superadmin/compliance accounts can call it; the server requires explicit consent, portal origin and CSRF, a bounded operational question, and a three-per-day limit. The gateway uses GPT-6 Astra/high reasoning with GPT-5.6 Sol/medium fallback, reserves cost before hosted calls, and validates analysis/risks/recommendations before returning advisory text. No model action or database write is exposed by this endpoint apart from audit/telemetry. API and web production builds, gateway controls, coach, portal, and operator endpoint checks passed with a simulated provider. Live OpenAI, PostgreSQL, and installed Ollama model validation remain outstanding. No API key is present in this environment.

2026-09-12 OpenAI routing checkpoint: The server-side gateway uses the Responses API for hosted OpenAI calls, with task-configured reasoning effort. Premium consultation routes to GPT-5.6 Sol at medium effort; deep routine optimization routes to GPT-6 Astra at high effort, with Sol fallback. Entitled coach requests can use Sol at low effort only after local candidates fail. Membership status is read from the portal store; free users cannot request hosted escalation. The coach cache partitions Premium and local answers. OpenClaw continues to use Ollama's local Chat Completions-compatible endpoint. Hosted calls require an API key supplied server-side; a ChatGPT subscription is not an API credential.

Validated without network charges: gateway adapter request/response simulation, model selection, entitlement, budget accounting, local OpenClaw compatibility, gateway regressions, API and web builds, coach and portal tests. OPENAI_API_KEY was unavailable in this environment, so a live Responses API call and account-specific model access remain unverified. The two Tier-3 task types are gateway capabilities; no public portal route currently calls either one directly. Hosted cost and token limits still need production observation and provider-side spend limits.

2026-09-12 reservation checkpoint: gateway build/regressions/release controls, API build, budget tests, portal/billing/operations tests, and web type checking passed. The local Ollama tags endpoint responded with zero installed models. DATABASE_URL, SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, and OPENAI_API_KEY were absent from the execution environment; no live database or hosted inference test was performed.

Hosted inference now requires an identified account and server-provided premium entitlement on every hosted candidate, including routine-task fallbacks. Budgets are rechecked before each attempt. Accounting or telemetry failures after inference propagate without repeating the provider call.

The admin dashboard converts stored cents into dollars and labels its counts as telemetry records: validation rejections can produce multiple records for one request. These counts are not unique request totals. Token estimates exclude local hardware and hosting costs.

Validated locally: compiled gateway release-control tests, gateway regressions, API build and referral integration tests, and web type checking.

Hosted attempts now reserve estimated cost atomically before calling the provider. Persistent reservations and spend updates share a transaction and a per-budget PostgreSQL advisory lock. Settlement is idempotent and uses the original UTC budget day. Local tests cover concurrent admission through two store instances, midnight settlement, invalid amounts, and gateway in-flight admission.

Reservations use UTF-8 prompt bytes plus a framing allowance and configured maximum output tokens with the existing price table. This is a conservative text estimate, not a guaranteed provider billing ceiling. Unknown, legacy or zero-priced hosted models fail closed. Successful responses settle from validated usage before content validation; missing usage or a mismatched returned model keeps the hold and fails without replay. Timeout/network failures retain their holds because billing is unknown, and each retry needs a new reservation. Older unresolved holds require manual provider-bill verification before reconciliation. Actual charges above the estimate are recorded honestly and can exceed the allowance; provider-side spending controls remain necessary.

Outstanding release work: actual hosted adapter compatibility checks; live Supabase/Postgres migration and cross-connection concurrency validation; operational concurrency and backup restore validation. Existing passing local tests do not establish production readiness.
